import { loginRequest } from './actions/uiActions';

const mapDispatchToProps = {
  displayNotificationDrawer,
  hideNotificationDrawer,
  loginRequest
};

function App(props) {
  const { loginRequest } = props;
  // Use loginRequest function
}
